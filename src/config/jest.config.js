   // jest.config.js
   module.exports = {
    transform: {
        '^.+\\.tsx?$': 'babel-jest', // Use Babel to transform TypeScript files
    },
    moduleFileExtensions: ['ts', 'js', 'json', 'node'],
};